{
  "name": "@shio/web",
  "version": "1.0.0",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "clean": "find -E . -regex '.*\\.(info|js|map|d\\.ts)' -not -path \"**/node_modules/*\" -not -path  \"./.next/*\" -delete && rm -rf dist",
    "test": "tsc && jest --config jest.unit.json",
    "postinstall": "tsc"
  },
  "dependencies": {
    "@shio/chatengine": "^1.0.0",
    "@types/express": "^4.16.1",
    "body-parser": "^1.19.0",
    "express": "^4.17.1"
  }
}
